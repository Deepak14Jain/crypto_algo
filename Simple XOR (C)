#include <stdio.h>

int main() {
    char mes[50];
    printf("Enter a message:");
    gets(mes);
    printf("\n");
    for(int i=0; i<strlen(mes); i++){
        printf("%c",mes[i]^0);
    }
    return 0;
}
